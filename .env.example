# TMDB API Configuration
# Get your API key from: https://www.themoviedb.org/settings/api
VITE_TMDB_API_KEY=your_api_key_here

# Convex Configuration
# Get your URL from: https://dashboard.convex.dev
VITE_CONVEX_URL=your_convex_url_here

# Convex Auth Configuration
# For local development: http://localhost:5173
# For production: your deployed site URL (e.g., https://yourdomain.com)
SITE_URL=http://localhost:5173

# ============================================
# CONVEX BACKEND ENVIRONMENT VARIABLES
# ============================================
# These variables are NOT read from this file!
# They must be set in Convex using the CLI or dashboard.
#
# To set Convex environment variables:
#   bunx convex env set VARIABLE_NAME "value"
#
# To view current Convex environment variables:
#   bunx convex env list
#
# Or manage them in the dashboard:
#   https://dashboard.convex.dev
# ============================================

# Email Verification (CONVEX BACKEND ONLY)
# This variable is listed here for documentation purposes only.
# To actually enable/disable email verification, run:
#   bunx convex env set REQUIRE_EMAIL_VERIFICATION "true"   # Enable verification
#   bunx convex env set REQUIRE_EMAIL_VERIFICATION "false"  # Disable verification
# 
# When enabled: Users must verify email before account is created
# When disabled: Users can sign up without email verification
# REQUIRE_EMAIL_VERIFICATION=false

# Video Player APIs Configuration
# Add as many players as you want by incrementing the number
# Format: Name|MovieURL or Name|MovieURL|TVURL (if TV has different URL structure)
# 
# Supported placeholders:
# {id}, {tmdb_id}, {media_id} - TMDB media ID
# {type}, {media_type} - "movie" or "tv"
# {season}, {s} - Season number (TV shows only)
# {episode}, {e} - Episode number (TV shows only)
#
# Examples with single URL (works for both):
VITE_PLAYER_API_1=VidSrc|https://vidsrc.xyz/embed/{type}/{id}/{season}/{episode}
# Examples with separate Movie|TV URLs:
# VITE_PLAYER_API_2=Pstream|https://iframe.pstream.mov/media/tmdb-movie-{tmdb_id}|https://iframe.pstream.mov/media/tmdb-tv-{tmdb_id}?s={season}&e={episode}
# VITE_PLAYER_API_3=Cinezo|https://api.cinezo.net/embed/tmdb-movie-{tmdb_id}|https://api.cinezo.net/embed/tmdb-tv-{tmdb_id}?s={season}&e={episode}
